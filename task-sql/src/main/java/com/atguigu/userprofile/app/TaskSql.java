package com.atguigu.userprofile.app;

/**
 * @ClassName: TaskSql
 * @Description: 处理SQL任务
 * @Author: fanfan
 * @DateTime: 2022年08月10日 11时34分
 * @Version: v1.0
 */
public class TaskSql {
    public static void main(String[] args) {
        // 1. 获取传递进来的taskId、busiDate(业务日期)
        // 约定好 (代码中已经写好了)，第一个参数是taskId，第二个参数是业务日期


        // 2.通过tasId从Mysql获取标签信息(标签编码、标签名等) 任务信息(sql) 标签规则信息(查询值和标签对应关系)

        // 3. 建表（如果不存在）

        //4. 拼接 insert select sql

        // 5. 执行SQL
    }
}
